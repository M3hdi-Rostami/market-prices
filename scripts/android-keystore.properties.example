# Copy to android/keystore.properties and fill in your passwords.
# Generate the keystore first: npm run setup:apk-signing
#
# NEVER commit keystore.properties or *.jks to git.

storeFile=keystore/market-prices-release.jks
storePassword=YOUR_STORE_PASSWORD
keyAlias=market-prices
keyPassword=YOUR_KEY_PASSWORD
